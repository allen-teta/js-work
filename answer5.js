function sumArray(arr){
    let total = 0;
    for (let i= 0; i < arr. length;i++){
      total +=arr[i];
    }
    return total;
  }
    const myArray = [1,2,3];
    const sum = sumArray(myArray);
    console.log(sum);